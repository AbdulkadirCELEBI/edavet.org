import antfu from '@antfu/eslint-config'

export default antfu({
  // Type: React, Vue, Svelte, UnoCSS, etc.
  svelte: true,
  typescript: true,
  // Add other configurations as needed
  formatters: true,
})
